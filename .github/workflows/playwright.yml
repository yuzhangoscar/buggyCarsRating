name: Playwright Tests

on:
  # Nightly run at midnight UTC
  schedule:
    - cron: "0 0 * * *"

  # Manual trigger with environment selection
  workflow_dispatch:
    inputs:
      environment:
        description: "Test environment"
        required: true
        default: "dev"
        type: choice
        options:
          - dev
          - staging
          - qa

env:
  API_BASE_URL: "https://placeholder.example.com/api"
  APP_USERNAME: "placeholder_user"
  APP_PASSWORD: "placeholder_password"
  # GitHub Actions → Settings → Secrets: add BUGGY_CARS_LOGIN_PASSWORD (not committed).
  BUGGY_CARS_LOGIN_PASSWORD: ${{ secrets.BUGGY_CARS_LOGIN_PASSWORD }}

jobs:
  test:
    name: Run Playwright Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies on runner
        run: npm ci --ignore-scripts

      - name: Build Docker image
        run: make docker-build

      - name: Run lint in Docker
        run: make docker-lint

      - name: Restore rotation state
        uses: actions/cache/restore@v4
        with:
          path: .playwright/comment-allocation-state.json
          key: comment-rotation-state-${{ github.run_id }}
          restore-keys: comment-rotation-state-

      - name: Create output directories
        run: mkdir -p allure-results test-results .playwright

      - name: Run Playwright tests in Docker
        run: make docker-test TEST_ENV=${{ github.event.inputs.environment || 'dev' }}

      - name: Fix output directory permissions
        if: always()
        run: sudo chown -R $(id -u):$(id -g) allure-results test-results .playwright 2>/dev/null || true

      - name: Upload test results (screenshots)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results/
          if-no-files-found: ignore
          retention-days: 30

      - name: Upload Allure results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: allure-results/
          if-no-files-found: warn
          retention-days: 30

      - name: Fetch Allure history from gh-pages
        if: always()
        run: |
          git fetch origin gh-pages --depth=1 || true
          if git rev-parse --verify origin/gh-pages >/dev/null 2>&1; then
            git checkout origin/gh-pages -- history 2>/dev/null || true
            if [ -d history ]; then
              mkdir -p allure-results/history
              cp -r history/* allure-results/history/
              rm -rf history
            fi
          fi

      - name: Generate Allure report with history
        if: always()
        run: rm -rf allure-report && npx allure generate -o allure-report

      - name: Upload Allure report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report/
          if-no-files-found: warn
          retention-days: 30

      - name: Save rotation state
        if: always()
        uses: actions/cache/save@v4
        with:
          path: .playwright/comment-allocation-state.json
          key: comment-rotation-state-${{ github.run_id }}

      - name: Deploy Allure report to GitHub Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: allure-report
          keep_files: false
